name: Docker Image CI

on:
  push:
  pull_request:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file C:\VS-Code\git\uek223-team2-blogpost-frontend_official\Dockerfile --tag asny2023/frontend:$(date +%s)
